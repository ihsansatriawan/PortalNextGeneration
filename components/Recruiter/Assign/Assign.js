import React, {useState} from 'react';
import {
    Form,
    Input,
    Tooltip,
    Icon,
    Cascader,
    Select,
    Row,
    Col,
    Checkbox,
    Button,
    AutoComplete,
    message,
    Table,
    Radio,
    DatePicker,
    Upload,
    Skeleton
} from "antd";
import { fetch } from '@helper/fetch';



const Assign = (props) => {


    

    return (
        <Input></Input>
    )
}

export default Assign;